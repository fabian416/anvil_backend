---
description: Secure Python development rules for FastAPI backend
globs: **/*.py,**/*.ipynb,**/*.pyw
alwaysApply: true
---

# Secure Python Development

These rules apply to all Python code in the repository and aim to prevent common security risks through disciplined use of input validation, output encoding, and safe APIs.

All violations must include a clear explanation of which rule was triggered and why, to help developers understand and fix the issue effectively.

**If code is in violation, add a comment explaining why. Do not generate code that violates these rules.**

## 1. Do Not Use User Input in File Paths
- **Rule:** Never use `input()`, `request.args`, `request.form`, `request.query_params`, `request.body`, `sys.argv`, or similar sources directly in file paths or file operations.
- **FastAPI Context:** Use Pydantic models for request validation, then validate file paths separately.

## 2. Always Sanitize and Validate External Input
- **Rule:** All external input must be sanitized and validated before being used in logic, database queries, file access, or rendering.
- **FastAPI Context:** Use Pydantic models for request validation. Validate business rules in application layer.

## 3. Avoid `eval`, `exec`, and `compile` on Dynamic Input
- **Rule:** Do not use `eval()`, `exec()`, or `compile()` on any user-controllable input.

## 4. Use Constant-Time Comparison for Secrets
- **Rule:** Use `hmac.compare_digest()` for comparing secrets like tokens, passwords, or signatures to prevent timing attacks.
- **FastAPI Context:** Use `bcrypt` for password hashing (already in use). Use `hmac.compare_digest()` for token comparisons.

## 5. Do Not Log Sensitive Data
- **Rule:** Logs must not contain passwords, tokens, API keys, private keys, or personally identifiable information (PII).
- **FastAPI Context:** Be careful with request/response logging. Sanitize before logging.

## 6. Avoid Subprocess Calls with User Input
- **Rule:** Avoid using `os.system`, `subprocess.run`, or similar functions with user input. Use parameterized APIs or sandboxed environments if needed.
- **FastAPI Context:** If subprocess is needed, use `subprocess.run()` with `args` list, not shell=True.

## 7. Escape Output in Web Contexts
- **Rule:** When rendering user-generated content into HTML, JSON, or command-line output, always apply appropriate escaping.
- **FastAPI Context:** FastAPI automatically escapes JSON. For HTML responses, use Jinja2 templates with auto-escaping enabled.

## 8. Avoid Hardcoded Secrets
- **Rule:** Do not hardcode passwords, tokens, or secret keys in source code. Use environment variables or a secure configuration service.
- **FastAPI Context:** Use TOML configuration system. Store secrets in `.secrets.toml` (not tracked in git) or environment variables.

## 9. Restrict Dynamic Imports
- **Rule:** Avoid `__import__()` or `importlib` with dynamic or user-controlled values.

## 10. Avoid Loose Type Comparisons in Security Logic
- **Rule:** Use strict type checks and avoid logic that relies on implicit truthiness (e.g., `if token == 0:`) when handling authentication or access control.
- **FastAPI Context:** Use proper type hints and explicit None checks for authentication logic.

## 11. Do Not Use `pickle` with Untrusted Data
- **Rule:** Do not load or deserialize untrusted data using `pickle`, `cPickle`, or `dill`. Use safe formats like `json` or `pydantic` for structured data exchange.
- **FastAPI Context:** Use Pydantic models for data validation and serialization. Use `orjson` for JSON serialization (already in use).

## 12. SQL Injection Prevention
- **Rule:** Always use parameterized queries. Never construct SQL queries by string concatenation with user input.
- **FastAPI Context:** Use SQLAlchemy ORM with parameterized queries. Never use raw SQL with string formatting.

## 13. Input Validation in Presentation Layer
- **Rule:** Validate input structure and types in presentation layer (Pydantic models), but validate business rules in application/domain layers.
- **FastAPI Context:** Use Pydantic request models for structural validation. Business rule validation belongs in interactors.

## References
- Secure SQL usage: [secure-sql-usage.mdc](mdc:.cursor/rules/secure-sql-usage.mdc)
- Path traversal prevention: [path-traversal-prevention.mdc](mdc:.cursor/rules/path-traversal-prevention.mdc)
- SSRF prevention: [ssrf-prevention.mdc](mdc:.cursor/rules/ssrf-prevention.mdc)
